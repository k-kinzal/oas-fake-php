openapi: 3.0.0
info:
  title: Versioned Petstore API
  version: 1.0.0
servers:
  - url: https://api.versioned.example.com/v1
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        '200':
          description: A list of pets
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - id
                    - name
                  properties:
                    id:
                      type: integer
                    name:
                      type: string
